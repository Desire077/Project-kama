{
  "name": "project-kama",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build:all": "npm --prefix frontend run build && echo Backend no build step",
    "test:all": "echo \"Run frontend and backend tests separately\"",
    "start": "node backend/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "axios": "^1.13.0",
    "mongodb": "^6.20.0"
  }
}
